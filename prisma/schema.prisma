// generator client {
//   provider = "prisma-client-js"
// }

// datasource db {
//   provider = "mongodb"
//   url      = env("DATABASE_URL")
// }

// // Images model 
// model Images {
//   id      String  @id @default(auto()) @map("_id") @db.ObjectId
//   file_id  String  
//   url     String  
//   UserId    String?@db.ObjectId @unique
//   users   users? @relation(fields: [userId], reference: [id])
// }


// // User model
// model User {
//   id       String  @id @default(auto()) @map("_id") @db.ObjectId
//   name     String
//   email    String  @unique
//   password String? // Password optional hai
//   following String[]
//   createdAt DateTime @default(now())
//   updatedAt DateTime @updatedAt
//   avatar   Images? // @relation(fields: [avatarId], references: [id])
//   avatarId String? @unique @db.ObjectId
// }



// prisma/schema.prisma
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mongodb"
  url      = env("DATABASE_URL")
}

// User model 
model User {
  id        String   @id @default(auto()) @map("_id") @db.ObjectId
  name      String
  email     String   @unique
  password  String?
  following String[]
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  
  // Relation 
  avatar    Images?  @relation(fields: [avatarId], references: [id])
  avatarId  String?  @unique @db.ObjectId
}

// Images model 
model Images {
  id      String  @id @default(auto()) @map("_id") @db.ObjectId
  file_id String
  url     String
  
  
  User    User?
}